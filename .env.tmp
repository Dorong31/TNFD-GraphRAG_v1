# TNFD-GraphRAG 환경 설정 예제
# 이 파일을 복사하여 .env 파일로 저장하고 값을 설정하세요

# ============ LLM API Keys ============
# Google Gemini API 키 (필수 - LLM 및 임베딩 모두 사용)
GOOGLE_API_KEY=your-google-api-key-here

# ============ Neo4j Database (Docker 로컬) ============
# Neo4j 연결 URI (로컬 Docker)
NEO4J_URI=neo4j://localhost:7687

# Neo4j 인증 정보
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-neo4j-password-here

# ============ Model Configuration ============
# LLM 모델명 (답변 생성용 - 768차원 컨텍스트)
LLM_MODEL=gemini-3-flash-preview

# 임베딩 모델명 (768차원 출력)
# gemini-embedding-001은 gemini-3-flash-preview와 동일한 768차원 사용
EMBEDDING_MODEL=models/gemini-embedding-001

# ============ Graph Settings ============
# Graph Traversal 깊이 (hop 수)
GRAPH_TRAVERSAL_DEPTH=2

# 검색 결과 상위 K개
TOP_K_RESULTS=5

# ============ Neo4j Docker 실행 명령어 ============
# 아래 명령어로 Neo4j Docker 컨테이너를 실행하세요:
#
# docker run -d \
#   --name neo4j-tnfd \
#   -p 7474:7474 \
#   -p 7687:7687 \
#   -e NEO4J_AUTH=neo4j/your-password \
#   -e NEO4J_PLUGINS=["apoc"] \
#   -v neo4j_data:/data \
#   neo4j:5.26-community
#
# 웹 브라우저에서 http://localhost:7474로 접속하여 확인
